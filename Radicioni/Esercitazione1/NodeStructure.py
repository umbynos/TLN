# Node structure containing words and similarity misure

class Node:
	def __init__(self, fst_word, snd_word):
		self.value = value
		self.left_child = None
		self.right_child = None
		#leaf used to keep the value of the words
		self.leaf_child = None 